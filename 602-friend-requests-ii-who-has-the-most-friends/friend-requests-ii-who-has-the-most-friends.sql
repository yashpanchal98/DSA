SELECT ID, COUNT(ID) AS num
FROM (

    SELECT REQUESTER_ID AS ID
    FROM REQUESTACCEPTED 

    UNION ALL
    SELECT ACCEPTER_ID AS ID
    FROM REQUESTACCEPTED

) AS u
GROUP BY ID
ORDER BY num DESC
LIMIT 1;
